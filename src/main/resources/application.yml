server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: gramgo

  datasource:
    url: jdbc:mysql://gramgo.ctwwi48aqroc.eu-north-1.rds.amazonaws.com:3306/vehicle_rental_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: admin
    password: Navii2703
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false

  data:
    mongodb:
      uri: mongodb+srv://naveetha:%40navii27@cluster0.w58fep3.mongodb.net/vehicle_rental_logs?retryWrites=true&w=majority&appName=Cluster0
      auto-index-creation: true

    redis:
      host: localhost
      port: 6379
      timeout: 2s

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:naveetha2703@gmail.com}
    password: ${MAIL_PASSWORD:kvkdnpmxumnqwpdn}
    default-encoding: UTF-8
    test-connection: false
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 10000
          timeout: 10000
          writetimeout: 10000

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: ${JWT_SECRET:change-me-in-production-use-a-long-random-string-32chars-min}
  expiration: 900000
  refresh-expiration: 604800000
  issuer: gramgo

security:
  login:
    max-failed-attempts: 5
    lock-minutes: 30
  otp:
    ttl-minutes: 10
    resend-window-seconds: 60

razorpay:
  key-id: ${RAZORPAY_KEY_ID:rzp_test_REPLACE_WITH_YOUR_KEY_ID}
  key-secret: ${RAZORPAY_KEY_SECRET:REPLACE_WITH_YOUR_KEY_SECRET}
  currency: ${RAZORPAY_CURRENCY:INR}

app:
  upload:
    dir: uploads
    base-url: http://13.60.66.231:8080
  frontend:
    url: http://gramgo-git.vercel.app

logging:
  level:
    com.eventbooking: DEBUG
    org.springframework.security: INFO
    org.hibernate.SQL: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/vehicle-rental.log

management:
  health:
    redis:
      enabled: false
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized